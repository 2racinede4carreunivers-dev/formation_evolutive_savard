# ╔══════════════════════════════════════════════════════════════════════╗
# ║  WORKFLOW NEUTRALISE — Code conserve pour visualisation            ║
# ║  Ce workflow n'est plus actif sur ce depot (formation_evolutive).  ║
# ║  Les triggers automatiques sont desactives.                        ║
# ║  Le code source est preserve pour demontrer la structure et les    ║
# ║  capacites du systeme de Q&R evolutif aux recruteurs.              ║
# ╚══════════════════════════════════════════════════════════════════════╝
#
# Workflow automatique quotidien - Generation Q&R
# Theorie Mathematique Philippe Thomas Savard 2026
#
# Frequence originale: 3 fois par jour (6h, 12h, 18h UTC)
# Tache: Genere 1 question/reponse a chaque execution

name: "[ARCHIVE] Auto QR Quotidien"

on:
  # --- TRIGGERS NEUTRALISES ---
  # Les declencheurs automatiques ci-dessous sont desactives.
  # Decommenter les lignes suivantes pour reactiver :
  #
  # schedule:
  #   - cron: '0 6 * * *'
  #   - cron: '0 12 * * *'
  #   - cron: '0 18 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate_single_qa:
    runs-on: ubuntu-latest
    # --- JOB NEUTRALISE ---
    # Ce job ne s'executera pas. Retirer la ligne ci-dessous pour reactiver.
    if: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configuration Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installation des dependances
        run: |
          python -m pip install --upgrade pip
          pip install emergentintegrations --extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/

      - name: Verification de la banque existante
        run: |
          mkdir -p qa_bank
          if [ -f qa_bank/qa_bank.db ]; then
            echo "Banque existante trouvee"
          else
            echo "Nouvelle banque sera creee"
          fi

      - name: Generation automatique de 1 QR
        env:
          EMERGENT_LLM_KEY: ${{ secrets._CLE }}
          GITHUB_SHA: ${{ github.sha }}
        run: |
          python3 scripts/auto_generate_qa.py

      - name: Commit et push de la banque mise a jour
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase || git pull || true
          if [ -f qa_bank/qa_bank.db ]; then
            git add qa_bank/qa_bank.db
            if [ -f qa_bank/CATALOGUE.md ]; then
              git add qa_bank/CATALOGUE.md
            fi
            HOUR=$(date -u +%H)
            git diff --staged --quiet || git commit -m "Auto: +1 QR $(date +%Y-%m-%d) ${HOUR}h UTC"
            git push || (git pull --rebase && git push) || true
          fi

      - name: Resume
        run: |
          echo "## Generation QR Automatique" >> $GITHUB_STEP_SUMMARY
          echo "**Date:** $(date -u)" >> $GITHUB_STEP_SUMMARY
          echo "1 question/reponse generee et ajoutee a la banque" >> $GITHUB_STEP_SUMMARY
